# user secrets — never commit
providers.jsonc
key-pool-state.json

# a real config placed in the package root (local default; like providers.jsonc,
# not committed — copy from *.example.* templates instead).
agent-runner.toml
agent-runner.jsonc
agent-runner.json

# python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# test / tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# session-local planning artifacts
.claude/plans/

# editor
*.swp
.*.swo
